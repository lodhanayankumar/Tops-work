Tasks
1.
Download a free Bootstrap-based admin template (such as AdminLTE or BootstrapDash), and copy its CSS, JS, and image files into the correct static resource folders of a new Spring Boot project (e.g., src/main/resources/static/css, /js, /images).
2.
Create a JSP file named dashboard.jsp that uses JSTL and EL to display a welcome message with the logged-in user's name passed from a Servlet or Spring Controller.
3.
Integrate a navigation sidebar from your chosen admin template into your Spring Boot or Servlet-based app, and make sure the sidebar links work and load different JSP pages for Dashboard, Orders, and Profile sections.
4.
Render a dynamic table of Flipkart-style orders on a JSP page using JSTL <c:forEach> — display order ID, product name, and status, with the data coming from a Java List<Order> passed by your backend controller.<br><br><em><strong>Hint:</strong> Use EL to access order fields inside the loop.</em>
5.
Refactor your project folder structure to match a professional web app: keep all static assets in /static, all JSPs in /WEB-INF/views or /templates, and all Java controllers/servlets in a separate package. Document your structure with a screenshot or tree view.
