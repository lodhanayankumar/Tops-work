Tasks
1.
Create a simple Java web app with a login form (username only) and use the HttpSession API to store the logged-in user's name after successful login. Display a personalized welcome message on a separate welcome.jsp page that only appears if the session exists.
2.
Implement a 'Remember Me' feature using cookies: when the user checks a 'Remember Me' box during login, store their username in a cookie and auto-fill it the next time they visit the login page.<br><br><em><strong>Hint:</strong> Use response.addCookie() and request.getCookies() to manage cookies in your servlet.</em>
3.
Build a logout button on your welcome page that calls session.invalidate() and redirects the user back to the login page. After logout, ensure the user cannot access the welcome page by typing its URL directly.
4.
Demonstrate session tracking using hidden form fields: create a two-step feedback form (step 1: enter name, step 2: enter feedback). Pass the user's name from step 1 to step 2 using a hidden input field and display both values on the final 'Thank You' page.
5.
Write a short comparison (3-4 lines) between session.invalidate() and JWT-based token expiry for session management, focusing on how each method handles user logout and session expiration in web apps like Myntra or Flipkart.
