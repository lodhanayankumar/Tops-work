
Tasks
1.
Create a simple HTML form for adding a new playlist (playlist name, description) and use XMLHttpRequest in JavaScript to send the form data to a dummy /addPlaylist endpoint without reloading the page. Show a success message on response.
2.
Build a real-time username availability checker for a signup form like Instagram — as the user types a username, use the Fetch API to call a mock /checkUsername endpoint and display 'Available' or 'Taken' instantly below the input.<br><br><em><strong>Hint:</strong> Use fetch() inside the input's onkeyup event and update the DOM with the result.</em>
3.
Given a registration form for a food delivery app (email, password), implement asynchronous form submission using Fetch API so that the page does not reload, and display a loading spinner while waiting for the server response.<br><br><em><strong>Hint:</strong> Show and hide the spinner based on fetch() promise state.</em>
4.
Here's a broken JavaScript snippet for checking if an email exists using XMLHttpRequest:<br><br>var xhr = new XMLHttpRequest();<br>xhr.open('POST', '/checkEmail');<br>xhr.send('email=' + email);<br>if(xhr.status == 200) { alert(xhr.responseText); }<br><br>Find and fix the bug so that the alert only shows after the response is received.
5.
Use ChatGPT or Copilot to help you write a Fetch API function that checks if a phone number is already registered (simulate a /checkPhone endpoint) and integrates with a form on your page. Paste the AI-generated code, then explain in 2 lines how you modified it to fit your form.
