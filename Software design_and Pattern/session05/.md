Tasks
1.
Create a simple Java servlet named SongUploadServlet that allows users to upload an MP3 file representing their favorite song, using MultipartConfig to handle the upload and save the file to a local 'uploads' folder.
2.
Modify your SongUploadServlet to save only the file path (not the file data) in a MySQL database table called uploaded_songs, along with the username and original filename.
3.
Build a DownloadSongServlet that lets users download a previously uploaded MP3 file by providing its filename as a request parameter. Ensure the file is streamed from the uploads folder to the browser.
4.
Compare the pros and cons of storing uploaded files as blobs in the database versus saving only their file paths on disk, specifically for a Flipkart-style product image gallery. Write your answer in 5-6 bullet points.
5.
Use GitHub Copilot in your IDE to auto-generate the core file upload and download methods for your SongUploadServlet and DownloadSongServlet. Paste the Copilot-generated code and briefly describe any changes you made to fit your use case.<br><br><em><strong>Hint:</strong> Trigger Copilot suggestions by typing method signatures and comments describing the upload/download logic.</em>
