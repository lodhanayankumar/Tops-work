
Tasks
1.
Create a new GitHub repository named 'music-playlist-api', push a basic Java Spring Boot REST API (with just one endpoint like /hello), and enable GitHub Actions for the repo.
2.
Write a simple JUnit test for your /hello endpoint in the 'music-playlist-api' project and make sure it passes locally before pushing.
3.
Set up a GitHub Actions workflow file (.github/workflows/ci.yml) that runs your JUnit tests automatically on every push to the main branch.
4.
Configure your GitHub Actions workflow to build your Spring Boot app and deploy it automatically to Render.com whenever code is pushed to main.<br><br><em><strong>Hint:</strong> Use the Render deploy hook and add it as a step after the build and test jobs in your workflow.</em>
5.
Use ChatGPT or GitHub Copilot to help you write a GitHub Actions step that caches Maven dependencies for faster builds, then add this step to your workflow and explain in a comment how it improves CI speed.
