Tasks
1.
Sign up for a free Razorpay test account, generate API keys, and securely store them in your Spring Boot project using application.properties.<br><br><em><strong>Hint:</strong> Never hardcode API keys in Java code; use environment variables or config files.</em>
2.
Create a Java REST endpoint /create-order in your Spring Boot backend that sends a POST request to Razorpay's order creation API and returns the order_id in the JSON response.
3.
Build a simple HTML page with a 'Pay Now' button and use the Razorpay JavaScript SDK to open the payment popup using the order_id received from your backend.
4.
Implement a webhook endpoint /payment-status in your Spring Boot backend to receive and verify Razorpay payment events. Log the payment status and order_id for each event.<br><br><em><strong>Hint:</strong> Use Razorpay's signature verification to ensure the webhook is authentic.</em>
5.
Use ChatGPT to generate example JSON payloads for Razorpay's order creation and payment webhook responses. Test your backend endpoints by sending these payloads using Postman or curl.<br><br><em><strong>Hint:</strong> Ask ChatGPT: 'Give me a sample Razorpay payment webhook JSON payload.'</em>
