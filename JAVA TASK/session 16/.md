1. Create a Java class called Song with fields: title, artist, and duration. Store 5 Song objects in an ArrayList and print the details of each song.
2. Build a Java program that uses a HashSet to store unique usernames for a Zomato-style food review app. Add at least 7 usernames, including 2 duplicates, and display all unique usernames.
3. Simulate a Flipkart shopping cart by creating an ArrayList of Product objects (with name and price). Write a method to calculate and print the total cart value.
4. Use a HashMap to map Instagram usernames to their follower counts. Insert at least 5 entries, then write code to display the username(s) with the highest follower count.<br><br><em><strong>Hint:</strong> Loop through the entry set to find the max value.</em>
5. Explain in your own words (2-3 sentences) how using collections like ArrayList or HashSet in Java is similar to how apps like Spotify or BookMyShow manage lists of songs, movies, or users in memory before saving to a database.
