1.
Create a Java class named Playlist with a method addSong() that prints 'Song added to playlist' and another method checkSongCount() that prints the total number of songs added.
2.
Define a class called MovieTicket with an instance variable price and a static variable totalTickets. Write methods bookTicket(int amount) to increase totalTickets and checkTotalTickets() to print the current total.<br><br><em><strong>Hint:</strong> Use the static keyword for totalTickets so it is shared across all MovieTicket objects.</em>
3.
Write a class called FoodOrder with methods addItem(String itemName) and getOrderSummary() that keeps track of items added and displays the full order when called.
4.
Create a class called InstaProfile with instance variables username and followers, and a method increaseFollowers(int count) that adds to followers and prints the updated count.
5.
Relate the concept of classes and methods to a real app you use: Pick any one (like Zomato, Flipkart, Instagram) and list 3 possible classes (as if you were coding the app) and for each, write 2 method names that would perform CRUD-like operations.
