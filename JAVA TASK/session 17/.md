1.Create a HashMap in Java to store 5 food delivery order IDs (String) as keys and their order amounts (Double) as values, then print all key-value pairs using an iterator.
2.Build a LinkedHashMap to store 4 recent songs played on Spotify, where the key is the song title and the value is the artist name. Display the songs in the order they were added.
3.Given a HashMap of Flipkart product names (String) and their prices (Integer), write code to filter and print only those products with a price above 1000 using an iterator.<br><br><em><strong>Hint:</strong> Use entrySet() and Iterator to loop through the map and check each price.</em>
4.Write a Java function that takes a HashMap of Instagram usernames (String) and their follower counts (Integer), sorts the entries by follower count in descending order, and prints the top 3 users.<br><br><em><strong>Constraint:</strong> Do not use streams or third-party libraries for sorting.</em>
5.Explain in your own words how a HashMap in Java is similar to a database table, using an example from a Zomato order history (order ID as key, order details as value).
