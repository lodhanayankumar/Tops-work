1.
Create a Java class called Playlist with private fields: name (String), owner (String), and totalSongs (int). Add public getter and setter methods for each field, and write a main method to set and print all values.
2.
Build a Flipkart-style Product class with fields: productName (String), price (double), and discount (double). Make all fields private, provide public getters/setters, and add a method getFinalPrice() that returns price after applying discount.
3.
Create an abstract class called UPITransaction with an abstract method processTransaction(double amount). Then, make two subclasses: PaytmTransaction and PhonePeTransaction, each providing their own implementation of processTransaction that prints which app is processing the amount.
4.
Write a Java interface called Searchable with a method search(String keyword). Implement this interface in a MovieLibrary class that stores an array of movie names, and returns all movies containing the keyword in search().
5.
Explain in your own words how abstraction in Java (using abstract classes or interfaces) acts like an API contract between the service layer and database layer in a Zomato-style food ordering app. Give one example for each (abstract class and interface) showing what methods would be exposed.
