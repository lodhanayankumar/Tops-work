1.
Create a Java class named Product with two attributes: productName (String) and price (double). Create an object of Product in your main method and print its details.
2.
Build a Java class called Playlist that represents a Spotify playlist with attributes: playlistName (String) and totalSongs (int). Add a method displayInfo() that prints both attributes in a user-friendly format.
3.
Imagine you are designing a Flipkart shopping cart. Create a CartItem class with attributes: itemName (String) and quantity (int). Write a method increaseQuantity() that adds 1 to the quantity and prints the updated value.<br><br><em><strong>Hint:</strong> Call increaseQuantity() twice from main and observe the output.</em>
4.
Create a Java class called MovieTicket for a BookMyShow-style app, with attributes: movieTitle (String) and seatNumber (String). Add a constructor to initialize both attributes and a method printTicket() to display the ticket details.
