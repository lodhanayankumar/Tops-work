1.
Create a Java class called Product with three constructors: a default constructor, a parameterized constructor that takes name and price, and a copy constructor that copies another Product object. In your main method, create one Product using each constructor and print their details.
2.
Build a class Movie with overloaded addReview methods: one that takes only a rating (int), and another that takes both a rating (int) and a review comment (String). Demonstrate adding reviews using both methods for a Movie object.
3.
Simulate a Flipkart-style cart item insertion by creating a CartItem class with overloaded constructors: one for just productId, one for productId and quantity, and one that copies an existing CartItem. Show how each constructor could represent a different way of adding an item to the cart.
4.
Refactor the following code to use constructor chaining in the class Playlist, which has three constructors: default, one with name, and one with name and description. Ensure that each constructor calls another constructor using 'this()' where appropriate.<br><br><em><strong>Hint:</strong> Avoid repeating code for setting the name and description fields.</em>
5.
Imagine you are adding new ways to register users in a Zomato-style app. Use method overloading in a Registration class to create multiple registerUser methods: one with just email, one with email and password, and one with email, password, and phone number. Implement and call each method in your main method.
