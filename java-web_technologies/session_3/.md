
Tasks
1.
Create a simple HTML form called search.html that asks the user for a song name and artist (like searching on Spotify), then submits the data to a Java Servlet using the GET method.
2.
Write a Java Servlet named SongSearchServlet that overrides the doGet() method to receive the song name and artist from the request, then displays a message like 'Searching for [song] by [artist]' in the browser.
3.
Build a feedback form for a food delivery app (like Zomato) that collects the user's name, order ID, and feedback message, and submits the data to a Java Servlet using the POST method.
4.
Implement a FeedbackServlet that overrides the doPost() method, retrieves the user's name, order ID, and feedback using request.getParameter(), and displays a thank you message with the submitted details.
5.
Modify your FeedbackServlet to check if any of the form fields are empty and display an error message if so; otherwise, show the thank you message.<br><br><em><strong>Hint:</strong> Use request.getParameter() and simple if-else logic to validate the inputs.</em>
