Tasks
1.
Draw a simple flow diagram (hand-drawn or digital) showing how a Flipkart-style product search page sends a request from the browser to a Java Servlet backend, which then fetches data from a database and sends it back to the browser.<br><br><em><strong>Hint:</strong> Label each step with HTTP request/response arrows and note which part is client, server, and database.</em>
2.
Open your browser's DevTools (Network tab) and visit any dynamic page on Zomato or Myntra. Identify and write down one HTTP request URL, its request method (GET/POST), and the response status code you see when searching or filtering items.
3.
Create a static HTML file called profile.html that displays your name, favorite app, and a short bio. Open it in your browser and note how the content always stays the same unless you edit the file.
4.
List 3 differences between static and dynamic web pages, using examples from apps like Instagram (dynamic) and a basic About Us page (static).
5.
Imagine you are building a BookMyShow-like ticket booking page. Briefly describe (in 3-4 steps) how the data flows from the user's browser to the Java Servlet backend and then to the database when a user books a ticket.
