Session 4: Select Queries & ResultSetTrainer Coverage:ExecuteQuery(), traverse ResultSet, display formatted output.Example: “Show all expenses for a given user.”Discuss pagination and sorting logic.

Tasks
1.Write a Java program using JDBC that connects to your MySQL database and executes a SELECT query to fetch all products from a 'products' table, then displays each product's name and price in a formatted table in the console.
2.Modify your code to accept a category name as user input and display only the products from that category, showing their names and prices using ResultSet traversal.<br><br><em><strong>Hint:</strong> Use a PreparedStatement to prevent SQL injection.</em>
3.Implement pagination for the products list: fetch and display only 5 products per page, and allow the user to enter a page number to view the corresponding set of products.
4.Add sorting to your SELECT query so that products are displayed in ascending order of price. Let the user choose whether to sort by price or by product name before fetching results.
5.Suppose you want to display a user's playlist from a 'playlists' table like Spotify does. Use ChatGPT or Copilot to generate a JDBC SELECT query and Java code snippet that fetches all songs for a given playlist_id, then adapt the generated code to display song titles and artists in your console output.
