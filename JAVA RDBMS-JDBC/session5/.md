Session 5: Transactions & Batch ProcessingTrainer Coverage:Auto-commit, commit, rollback in JDBC.Executing multiple inserts in a batch.Example: Upload multiple expense records at once.
Tasks
1.Create a Java program that connects to a MySQL database using JDBC, inserts a single new record into a 'playlist' table (id, song_name, artist) for a Spotify-style music app, and commits the transaction.
2.Write a Java method addMultipleMovies() that takes a list of movie names and inserts them into a 'movies' table using JDBC batch processing, similar to uploading multiple movies to a BookMyShow-style app.<br><br><em><strong>Hint:</strong> Use addBatch() and executeBatch() methods to perform the inserts efficiently.</em>
3.Simulate a Flipkart-style shopping cart: Write Java code that adds multiple products to an 'orders' table in a single batch using JDBC, but if any insert fails (e.g., duplicate product id), rollback the entire transaction so no products are saved.<br><br><em><strong>Hint:</strong> Use setAutoCommit(false), commit(), and rollback() methods for transaction control.</em>
4.Given this scenario: you forgot to call commit() after inserting multiple records in a JDBC transaction (auto-commit is off), explain what happens to the data in the database and how you would fix the issue in code.
